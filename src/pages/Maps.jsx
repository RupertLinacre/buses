import LeafletMap from '../components/LeafletMap';

const Maps = () => {
    return (
        <div className="h-screen w-full p-0 m-0">
            <LeafletMap />
        </div>
    );
};

export default Maps;