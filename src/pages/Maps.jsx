import LeafletMap from '../components/LeafletMap';

const Maps = () => {
    return (
        <div style={{ height: '100vh', width: '100%', padding: 0, margin: 0 }}>
            <LeafletMap />
        </div>
    );
};

export default Maps;